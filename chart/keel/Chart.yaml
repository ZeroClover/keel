apiVersion: v1
name: keel
description: Open source tool for automating Kubernetes deployment updates.
# The chart `version` and `appVersion` here are placeholders for local
# development. On release, both fields are overwritten from the pushed
# git tag by .github/workflows/ci.yml — push a tag like `vX.Y.Z` to build
# the image and publish the matching chart in a single run.
version: 0.0.1
appVersion: 0.0.1
keywords:
- kubernetes deployment
- helm release
- continuous deployment
home: https://github.com/ZeroClover/keel
sources:
- https://github.com/ZeroClover/keel
icon: https://raw.githubusercontent.com/ZeroClover/keel/master/static/keel-logo.png
